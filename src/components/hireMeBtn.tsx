const HireMe = () => {
  return (
    <button className="p-2 bg-hireMeBg rounded-md text-white font-semibold flex justify-center items-center gap-2 dark:bg-black">
      <i className="fi fi-rr-add flex justify-center items-center"></i>Hire Me
    </button>
  );
};

export default HireMe;
